extend layout

include mixins/_tripCard
include mixins/_pagination

block content
    .content.content--background
        .container.container--trips
            header.header
                h2.heading-secondary
                    span.color--secondary your upcoming
                    |  Trips
            .trips
                //- TODO each trip in trips
                +tripCard()
                +tripCard()
                +tripCard()
                +tripCard()
                +tripCard()
            +pagination()
